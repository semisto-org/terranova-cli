// Package msg — les messages français du CLI, externalisés (ISC-430).
// Règle : les noms de commandes, drapeaux, clés JSON, chemins d'API et valeurs
// d'énums restent dans le code (ils sont la structure, figée par .surface) ;
// tout texte destiné à un humain — aides du registre, résumés, erreurs,
// messages d'usage, en-têtes de tableaux — vit ici, et nulle part ailleurs.
package msg
